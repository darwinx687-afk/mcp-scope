# Reply Templates EN

Do not post automatically.

GitHub: https://github.com/darwinx687-afk/mcp-scope

## Thanks

Thanks for taking a look. The project is early, so specific feedback on report clarity, noisy findings, and missing config shapes is especially useful.

## No Secrets Reminder

Please do not paste secrets, full private configs, private paths, or internal hostnames publicly. A minimal redacted shape or report excerpt is enough.

## Static Only Clarification

MCP Scope is static-only in this preview. It does not execute MCP servers, request live `tools/list`, or call external AI APIs.

## False Positive Follow-Up

Could you share the rule ID, severity, and a minimal redacted example? I would like to tune noisy cases before adding bigger features.

## Config Shape Follow-Up

Could you share a minimal redacted JSON shape and what you expected `discover` or `scan` to output?

## Not In Scope

That request is outside the current local-first/static-only preview scope. I am noting it, but I do not want to add cloud or live execution behavior without an explicit consent design.
